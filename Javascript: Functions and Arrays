A)What is the purpose of return statement in a function ?
ANSWER:
Stops executing the function and returns the value 
B)What is the correct way of defining a function in JavaScript?
ANSWER:
Function keyword, Identifier and Parentheses 

C)What will the function 'test' return upon execution of these statements?
function test(a, b) {
    console.log( a + b );
}
test( 2 , 3 , 4);
ANSWER:5

D)What will the function test return upon execution of these statements?
function test(a, b, c) {
    console.log( a + b * c);
}
test(2,3);
ANSWER:NaN

E)In JavaScript function _____________ are hoisted
ANSWER:Declarations

F)What will be the output of following code?
x = 5;
console.log(x);
var x;
ANSWER:5

G)What is the output of following code?
hoisted();

function hoisted() {
    console.log('Hoisted');
}
ANSWER:"Hoisted"

H)What will be the output of following code?
function demo() {
    console.log(x);
    var x = 10;
}

demo();
ANSWER:undefined

I)What will the following code print on console?
function multiply(a, b) {
    return a*b;
};

console.log(multiply);
ANSWER:
ƒ multiply(a, b) { return a*b; } 

J)What is the output of following code?
var a = 10;

function test() {
    var a = 20;
}

test(); 
console.log(a);
ANSWER:10

K)What is the output of following code
var a = 10;

function test() {
    var a = 20;
    console.log(a);
}

test();
ANSWER:20

L)What is the expected output of following code
function a() {
    console.log("Inside a");

    function b() {
        console.log("Inside b");
    }
}

a();
ANSWER:
Inside a 

M)What is the expected output of following code
function a() {
    console.log("Inside a");

    function b() {
        console.log("Inside b");
    }
}

b();
ANSWER:
Uncaught ReferenceError: b is not defined 

N)What is the expected output of following code?
var combinedString = "";
function a() {
    combinedString = "Inside a";

    function b() {
        combinedString = combinedString + " Inside b";
    }

    b();
}

a();
console.log(combinedString);
ANSWER:
Inside a Inside b 

O)What is the output of following code?
var RectArea = function(width, height) {
    return width * height;
}

console.log(RectArea(5,4));
ANSWER:20

P)What is the expected output of following code?
function a() {
    console.log("Inside a");

    function b() {
        console.log("Inside b");
    }
}

a()b();
ANSWER:
Uncaught SyntaxError: Unexpected identifier

Q)What is the expected output of following code?
var add = function (a, b) {
    return a+b;
}

var subtract = function (a, b) {
    return a-b;
}

var op = function (func) {
    var x = 2;
    var y = 3;
    return func(x, y);
}
console.log(op(subtract));
ANSWER:-1 

R)What is the expected output of following code?
var add = function(a, b) {
    return a+b;
}

var subtract = function(a, b) {
    return a-b;
}

var op = function (func) {
    var x = 2;
    var y = 3;
    return func(x, y);
}

console.log(op(add));
ANSWER:5

S)How to find the length of an array?
ANSWER:arr.length

T)What is the output of the following code?
var color= ["Orange", "Blue", "Green"];

color.push("Red");

console.log(color[0]+ " " +color[color.length-1]);
ANSWER:
Orange Red 

U)What is the output of the following code?
var fruits = ["Red", "Orange", "Blue", "Green"];

console.log(fruits.shift());
ANSWER:
Red

V)Is the below given array allowed in Javascript?


var myArray = [45, "Ninja"];
ANSWER:Yes

W)What is the function of splice() in JavaScript?
ANSWER:
slices a given part of an array and returns that sliced part as a new array.

X)What is the output of following code?
var fruits = ['Apple', 'Orange', 'Kiwi', 'Strawberry'];

fruits.splice(4, 1, 'Banana');

console.log(fruits);
ANSWER:
["Apple", "Orange", "Kiwi", "Strawberry", "Banana"] 

Y)What is the output of following code?
var fruits = ['Apple', 'Orange', 'Kiwi', 'Strawberry'];

fruits.splice(1, 0, 'Banana');

console.log(fruits);
ANSWER:
["Apple", "Banana", "Orange", "Kiwi", "Strawberry"] 

Z)What is the output of following code?
var a = 10;

function test() {
    a = 20;
}

test();
console.log(a);
ANSWER:20

AA)Consider the code given below -
var arr = [ 1, , 3, 4 ];

What is the observation made?
ANSWER:
The omitted value takes “undefined”

AB)What will the following function return?
function sqSum(a, b) {

    function square(x) {
        return x*x;
    }

    return square(a) + square(b);

}
ANSWER:Sum of squuare of a and b

AC)What will the function 'test' return upon execution of these statements?
function test(a, b) {
    console.log( a + b );
}

test(2);
ANSWER:NaN

AD)What will be the output of following code snippet?
var a = 1;

function b() {
    a = 10;
    return;
    function a() { }
}

b();
console.log(a);
ANSWER:1

AE)What is the output of the following code?
array = [ 60, 70, 20, 10, 40, 90 ];

const test = function(x) {
    return x > 5;
}

if(!array.every(test)) {
    console.log("statement 1"); 
}

else {
    console.log("statement 2"); 
}
ANSWER:statement 2

AF)What is the output of following code?
hoisted(); 
var hoisted = function() {
    console.log('bar');
};
info
You have max 2 attempts to score in this question.
ANSWER:
TypeError: Hoisted is not a function

AG)What is the output of following code?
var words = ['one', 'two', 'three', 'four'];

words.forEach(function(word) {
    console.log(word);
    if (word === 'two') {
        words.shift();
    }
});
ANSWER:one two four

AH)What is the output of following code snippet?
var color = ['red', 'orange', 'blue', 'violet'];

var removed = color.splice(2);

console.log(color);
console.log(removed);
ANSWER:
["red", "orange"] ["blue", "violet"] 
